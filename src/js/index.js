import React from 'react';
import ReactDOM from 'react-dom';
import '../scss/index.scss';
import VisualizeReact from './App';

const element = document.getElementById('content');
ReactDOM.render(<VisualizeReact />, element);

